#include<stdio.h>
int main()
{
	int a = 0, b = 1, n , c = 0, i = 0;
	printf("Enter the number of terms : ");
	scanf("%d" , &n);
	while (i < n)
	{
		printf("%d " , a);
		c = a + b;
		a = b;
		b = c;
		i = i + 1;
	}
}
